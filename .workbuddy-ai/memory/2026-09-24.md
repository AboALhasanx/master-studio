# 2026-09-24 — Daily Work Log

## Bilingual PDF export — Week 02 File 01 (second test, after EN-only test on 2026-09-23)

Student asked to produce File 01 as a bilingual (EN+AR) PDF — the same file previously tested as English-only.

**Correct bilingual invocation (REUSABLE):** export the ORIGINAL bilingual markdown directly with `--lang ar` (or omit `--lang` — auto-detect sets "ar" because the file contains Arabic). Do NOT strip Arabic first.
- Command (canonical C: path):
  `C:/Users/gokoq/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe "C:/Users/gokoq/Master-Studio/90_Shared_Toolbox/tools/pdf_exporter.py" "C:/Users/gokoq/Master-Studio/01_Semester_1/04_Advanced_Software_Eng/03_Study_Notes/Week_02_File_01_SDLC_Fundamentals.md" -o "C:/Users/gokoq/Master-Studio/01_Semester_1/04_Advanced_Software_Eng/08_PDF_Exports/Week_02_File_01_SDLC_Fundamentals_Bilingual.pdf" --lang ar`
  NOTE: canonical vault path is now `C:\Users\gokoq\Master-Studio` (moved off Drive 2026-09-23). The first run of this command was accidentally done in the OLD `G:\My Drive\Master-Studio` path and had to be moved — ALWAYS use C: going forward.
- Result: **16 pages, 272 KB**. Verified via pypdf: 6610 Arabic chars present, 6/6 English key terms present (software life cycle, SDLC, software process, Verbatim, waterfall, requirements).

**Why `--lang ar` works for mixed content:** pipeline sets document `dir="rtl"`, but `tag_bilingual_blocks()` marks every Arabic-free block `dir="ltr"`, and `isolate_english_tokens_in_arabic()` wraps embedded English terms in `<bdi dir="ltr">`. So EN exposition renders LTR and AR explanation renders RTL inside one document. Callout titles default to Arabic when `lang=="ar"` (custom callout titles, if present, override).

**Contrast — EN-only (2026-09-23 test):** strip `**AR.**` blocks first, then export with `--lang en` → 6 pages, 169 KB; only Arabic is the hardcoded footer.

**Standing decisions (from 2026-09-23):** KEEP the hardcoded Arabic institutional footer (student chose this; it is the official University of Wasit name, unaffected by `--lang`).

**Reusable batch pattern (when student resumes):** loop Files 01–10.
- Bilingual: export original `03_Study_Notes/Week_02_File_0X_*.md` with `--lang ar`.
- EN-only: strip AR blocks (split on `\n\s*\n`, drop blocks matching `[\u0600-\u06FF\u0750-\u077F\u08A0-\u08FF\uFB50-\uFDFF\uFE70-\uFEFF]`), then `--lang en`.
- Both require `dangerouslyDisableSandbox: true` (tool launches Chromium). Dependencies already installed in managed venv: markdown, playwright, pypdf; Chromium-1243 present.

---

## LOCATION CORRECTION + BILINGUAL BATCH COMPLETE (same session, after above)

**Critical correction:** The vault's canonical location is `C:\Users\gokoq\Master-Studio` (moved off Google Drive File Stream on 2026-09-23). The bilingual File 01 test above was accidentally run against the OLD `G:\My Drive\Master-Studio` path. Student caught it: "we were supposed to be working in C:, yesterday we moved, but today's work wasn't done there — move it there."

**What was done to fix:**
- Moved today's outputs from G: to C:
  - `08_PDF_Exports/` (created in C:) now holds `Week_02_File_01_SDLC_Fundamentals_Bilingual.pdf`, `_EN.pdf`, `_EN.md`.
  - `2026-09-24.md` memory note moved to `C:\Users\gokoq\Master-Studio\.workbuddy-ai\memory\`.
- Cross-drive `Move-Item` from G:→C: was flaky on the large PDFs (Drive File Stream network mount); used copy + size-verify instead. Final `rm` of G: duplicates was BLOCKED by the safe-delete guard (trash op fails on the G: path — fail-closed, files preserved). So G: still holds stale duplicates of File 01's two PDFs; harmless.
- **All remaining work done in C: using the C: toolchain** (`C:/Users/gokoq/Master-Studio/90_Shared_Toolbox/tools/pdf_exporter.py`).

**Bilingual batch 02–10 (now COMPLETE in C:):**
- Generated `_Bilingual.pdf` for Files 02–10 via loop: `python pdf_exporter.py <C: source .md> -o <C: 08_PDF_Exports/_Bilingual.pdf> --lang ar`.
- Verified all 10 bilingual PDFs via pypdf: each has substantial Arabic (File 01=6610, 02=10672, 03=8230, 04=8615, 05=3809, 06=2727, 07=2193, 08=3202, 09=2346, 10=1019 Arabic chars) and 8–23 pages each. All OK.
- C: `08_PDF_Exports/` now contains: 10 bilingual PDFs (Files 01–10) + File 01's EN-only PDF + File 01's stripped EN markdown.

**Decision resolved:** student wanted bilingual (EN+AR) for the set, matching File 01. (The earlier open question — bilingual vs EN-only for the batch — is now answered: bilingual.) EN-only versions can still be generated on request via the strip+`--lang en` path.

**Standing rule going forward:** work ONLY in `C:\Users\gokoq\Master-Studio`. If a session starts in G:, stop and move. See MEMORY.md §3.

---

## ASE Week 02 — audit of the ten existing files (READ-ONLY, per student instruction)

**Student instruction (verbatim intent):** the ten Week 02 files already exist — **do NOT create any more files**; inspect them instead, and only if something is genuinely missing should we pull it from the source.

**Mistake made and corrected this session:** I launched three parallel subagents to re-author Units 03/04/05 as *new* files (`Week_02_Unit_03/04/05_*.md`). That was wrong — it duplicated existing content. All three were created at 11:36 and **deleted immediately** on the student's instruction. No duplicates remain. **Lesson: the ten files exist under the `Week_02_File_0X_` naming. Do not create parallel `Week_02_Unit_0X_` copies.**

**Audit result — content is COMPLETE, presentation is not:**

| File | KB | frontmatter | leak refs | `[THIN]` | `**EN.**` | `**AR.**` | Arabic chars | verbatim | diagram | retrieval |
|:---|---:|:---:|---:|---:|---:|---:|---:|---:|:---:|---:|
| 01 SDLC Fundamentals | 38.0 | yes | 0 | 0 | 0 | 0 | 7,164 | 40 | **1** | 12 |
| 02 BuildFix & Waterfall | 58.5 | yes | 5 | 6 | 5 | 37 | 10,082 | 63 | 0 | 16 |
| 03 Prototyping & Evolutionary | 44.4 | yes | 5 | 5 | 6 | 24 | 7,643 | 40 | 0 | 16 |
| 04 Incremental | 39.7 | yes | 5 | 3 | 5 | 19 | 8,189 | 24 | 0 | 16 |
| 05 RAD | 16.7 | **no** | 3 | 2 | 0 | 9 | 3,488 | 13 | 0 | 16 |
| 06 Spiral | 23.3 | **no** | 1 | 2 | 0 | 17 | 2,279 | 18 | 0 | 16 |
| 07 Unified Process | 19.8 | **no** | 1 | 8 | 0 | 16 | 1,894 | 15 | 0 | 16 |
| 08 Agile XP Scrum | 30.6 | **no** | 1 | 5 | 0 | 22 | 2,878 | 29 | 0 | 16 |
| 09 Choosing a Model | 24.3 | **no** | 1 | 2 | 0 | 13 | 1,886 | 17 | 0 | 9 |
| 10 Master Comparison | 61.2 | **no** | 2 | 1 | 0 | 0 | **0** | 0 | 0 | 133 |

- Retrieval sets exist in ALL ten — Files 05–09 use the `**[RS-0X-NN]**` + `> **Answer:**` format, not `**N.` (this is why a naive regex reports zero).
- `[VERIFY]` = 0 in every ASE note (those flags live in the Cyber Security notes, not here).
- Real gaps: **9 of 10 files have no diagram**; **Files 05–10 have no frontmatter** (so the new 3-tier centred PDF header cannot render); `File NN` / `BUILD_PLAN` references still leak in 02–10; `**EN.**`/`**AR.**` markers in 02–08.

**UNRESOLVED CONFLICT — do not decide unilaterally.** The `[THIN]` flags (35 occurrences) were **requested by the student on 2026-09-23** ("`[THIN]` flags where my summary is thinner than the source"), but `ACADEMIC_STUDY_NOTE_SOP.md` (2026-09-24) **bans** them from student-facing deliverables and says the substance must move into prose. **Both instructions cannot hold. Ask him which wins.**

## New tool built this session — `diagram_forge.py`

- `90_Shared_Toolbox/tools/diagram_forge.py` renders a self-contained HTML/SVG file to a **2x-retina PNG** via Playwright, matching the vault's established diagram house style (white ground, navy title, rounded accent-bordered cards, two-line footer).
- **Non-obvious fix:** Playwright's `chromium_headless_shell-*` build cannot be launched via `chromium.launch()`. The working binary is `%LOCALAPPDATA%\ms-playwright\chromium-*\chrome-win64\chrome.exe` — note **`chrome-win64`**, not `chrome-win`. `pdf_exporter.py` already resolves this correctly; `diagram_forge.py` now mirrors it.
- Run with the **system** Python 3.12 (`C:\Users\gokoq\AppData\Local\Programs\Python\Python312\python.exe`) — it has Playwright + PIL + pymupdf. The managed venv has Playwright but **no PIL**.
- HTML sources kept in `90_Shared_Toolbox/tools/diagram_src/` so any diagram can be regenerated.
- Four diagrams produced this session (they fill the missing-visual gap for Files 02–05): `waterfall_classical_vs_iterative.png`, `prototyping_vs_evolutionary.png`, `incremental_development_interleaving.png`, `rad_timeline_compression.png`.

## PDF export reminder (reconfirmed)

Exporting requires **sandbox disabled** (Chromium launch). All ten `_Bilingual.pdf` files already exist in `08_PDF_Exports/`, plus three superseded File 01 variants (`_new`, `_v2`, `_v3`) and two stale `_debug_inspect*` folders.

---

## ASE Week 02 Units 01–05 — SOP cleanup + diagrams + clean PDFs (DONE)

**Student brief:** turn Units 01–05 into PDFs "according to our rules and the formatter and the manifesto", read the old drafts, delete the old ones we don't need, make sure the figures show, one PDF per markdown file, **sequentially — no parallel work**.

**This also resolved the `[THIN]` conflict:** by saying "according to the manifesto", the student settled it — **the manifesto wins**, so `[THIN]` is removed. The *substance* was preserved as prose (`**ملاحظة تغطية:**` / `| **Coverage note** |`), never dropped.

### The cleaning recipe (REUSABLE — apply to Units 06–10 next)

Applied in place to `03_Study_Notes/Week_02_File_0N_*.md`, then exported. Line-by-line, in this order:
1. Drop `file: "0N of 10"` from frontmatter.
2. Drop the build-footer line matching `^\*File \d+ of \d+\. Built \d{4}`.
3. Rewrite the `> **Method.**` note (it advertised `[THIN]`).
4. `**[THIN]** — ` and `` `[THIN]` — `` at line start → `**ملاحظة تغطية:** `.
5. `| \`[THIN]\` |` table rows → `| **Coverage note** |`.
6. Strip `**EN.** ` / `**AR.** ` prefixes; drop lines that are only a marker.
7. `**Previous file:** File 0(N-1)` → `**Previous unit:** Unit 0(N-1)`; `The question it hands to File 0(N+1):` → `…to the next unit:`.
8. Global `\bFile\s+0?(\d{1,2})\b` → `Unit 0N` (zero-padded).
9. Fix the H1 and the frontmatter `title` separately — step 8 leaves `Unit 03 of 10`, so strip ` of 10` afterwards.

**Then per unit:** insert the diagram under a new subsection, followed by an Arabic "كيف تقرأ الرسم" table, and export:
`python 90_Shared_Toolbox/tools/pdf_exporter.py <md> -o 08_PDF_Exports/<name>.pdf -t study_pack --lang ar` (**sandbox disabled**).

### Results

| Unit | Pages | Figure page | Leakage |
|:--|--:|--:|:--|
| 01 SDLC Fundamentals | 17 | 11 | none |
| 02 Build & Fix / Waterfall | 24 | 16 | none |
| 03 Prototyping / Evolutionary | 20 | 13 | none |
| 04 Incremental Development | 18 | 5 | none |
| 05 RAD | 10 | 6 | none |

Unit 05 needed the most work: it had **no frontmatter, no "Where this sits"**, and a `**Position in the series:**` blockquote instead. Added frontmatter, the 3-part spine, an English Feynman analogy (§1.0, the dinner-rush kitchen), and the closing footer.

### FALSE POSITIVE to remember
`"of 10"` appearing in an exported PDF is usually the **page footer** (`Page 5 of 10`), not leakage. Filter on lines containing `Page` before reporting a leak.

### Environment gotcha — the safe-delete guard and locked files (cost ~10 minutes)
The vault's safe-delete guard routes `rm` / `os.unlink` / `shutil.move` through the Windows Recycle Bin and is **fail-closed**. When a file is **open in a PDF viewer**, the trash operation aborts with
`[safe-delete][SAFE_DELETE_FAIL_CLOSED] … trash-failed … "Some operations were aborted"`,
and the real cause is only visible in the Python traceback: `PermissionError: [WinError 32] The process cannot access the file because it is being used by another process`.
- `rm -rf` on such a file **hangs indefinitely** (it auto-backgrounds and never completes) — do not wait on it.
- **Detection:** `shutil.move` each file in a `try/except PermissionError` and report the locked ones instead of retrying.
- **Workaround used:** moved the 11 unlocked items to `99_Archives/2026-09-24_ASE_W02_superseded_exports/` (gitignored). Three File-01 variants stayed locked and remain on disk; they are now **untracked and gitignored** so they never reach the public repo.

### Git
Three commits pushed: `7787ab1` (cleanup + diagrams + PDFs), `8ba5bf2` + final (repo hygiene). `.gitignore` gained `_workbuddy_inspect/`, `_pdf_qa/`, `cyber_w01_pdf_check/`, `_debug_inspect*/` and the three locked File-01 variants. **Watch out:** `git add -A <folder>` will re-track an untracked-but-not-ignored file — that is how the three variants came back once. Ignore rules, not just `git rm --cached`, are needed.

---

## POST-MORTEM: the five PDFs I delivered had rendering defects (student fixed them with another agent)

**Student's instruction this turn: read the files, read the directives, understand the pre-build / during-build / post-build verification — and *change nothing*.** This entry is the result. No vault file was modified.

### The four defect classes and their true root causes

| # | Symptom (student's report) | Root cause | Fix applied |
|:--|:--|:--|:--|
| 1 | **Fake "Q" box** in the lecture body — u2 p16 (over the comparison section), u3 p6 & p12, u4 p7 & p11 | `transform_qa_cards()` ran a `re.DOTALL` regex over the **whole HTML document**. Any `<p><strong>N. text</strong></p>` followed by a `<blockquote>` matched — including ordinary numbered Arabic lists in the lecture body — and was converted to a `.qa-card` with the fallback `Q` badge. | The function now **partitions the document at the `## Retrieval set` heading** and transforms only what follows it. No retrieval heading ⇒ body returned untouched. |
| 2 | **Retrieval set rendered as raw blockquotes with `Answer:` text** — u5 p9 | The old QA regex accepted only `Q\d*` / `\d+` / `س\d*` badges. My `**[RS-05-01]**` format matched nothing. | Unified pattern now accepts `RS-[\d\-]+`, and a cleaner strips a leading `Answer:` / `الإجابة:` so the card carries no label clutter. |
| 3 | **Bold leaked across an entire page + asterisk distortion** — u3 p6 & p12, u4 p7 & p11 | **I wrote `***Change avoidance**` — three opening asterisks, two closing.** Unbalanced. Manifesto Law 5: an unclosed asterisk escapes the blockquote and bolds everything after it. Also malformed quote delimiters like `change.*"*`. | `note_linter.py` auto-balances `***Text**` → `**Text**`, `***Text*` → `*Text*`, and cleans `*."*` / `*"*` / `."*` → `."`. |
| 4 | **"Fake Q box at top; bold quote font"** — u4 p7 | Same as #3 — the leaked bold made the Sommerville citation and the following Arabic paragraphs render bold. | Same. |

**Authoring rule extracted:** inside a `*"…"*` verbatim quote, every bold span must open **and** close with exactly `**`. Never `***`. And never place a bold numbered line immediately above a blockquote outside the retrieval set.

### The process step I skipped — and it is the whole lesson

I ran: clean → diagram → **export** → verify.
The vault requires: clean → diagram → **`note_linter.py --fix --strict`** → export → **render pages and look at them** → verify.

`ACADEMIC_STUDY_NOTE_SOP.md` (11:11) already mandated "Autonomous 1-click publishing" — but the **hard gate did not exist yet when I built**. The other agent created it *in response to my defects*:
- `90_Shared_Toolbox/tools/note_linter.py` — 12:23 (untracked)
- `00_STUDIO_HUB/STUDY_NOTE_MANIFESTO.md` — 12:30 (untracked)
- `AGENTS.md` §6.0.1 — new "Inviolable Zero-Defect Delivery Gate" (uncommitted)
- `pdf_exporter.py` — QA-card scoping fix (uncommitted)

### The three-phase discipline to internalise

**Before building** — read, in this order: `AGENTS.md` → `00_STUDIO_HUB/STUDY_NOTE_MANIFESTO.md` → `guides/ACADEMIC_STUDY_NOTE_SOP.md` → `guides/PDF_PUBLISHING_SOP.md` → `templates/template-study-unit.md`. Know the 7 Manifesto Laws and the 9 Engine Laws.

**During building** — Law 5 (balanced asterisks, no `***`), Law 3 (Q&A cards vs citations), Law 6 (zero scaffolding), Law 7 (no ASCII art). Retrieval items use `**[RS-XX-YY]**` or `**N.` with the answer in a `>` blockquote directly beneath.

**Before delivery** — the gate, in order:
1. `python "90_Shared_Toolbox/tools/note_linter.py" "<note>.md" --fix --strict` → **must exit 0**. Exit 1 = do not deliver.
2. `python "90_Shared_Toolbox/tools/pdf_exporter.py" "<note>.md" -t study_pack`
3. `PDF_PUBLISHING_SOP.md` §6 — the 7-point checklist (file exists & >50 KB, file-lock notice, page count, no leaked `$…$`/HTML, no prompt artifacts, table wrapping, citation-vs-Q&A differentiation).
4. **Render the pages with PyMuPDF and actually look at them.** The linter cannot see a fake Q box or a leaked bold — only eyes on the page catch those. That is exactly what the other agent did, saving renders to `_workbuddy_inspect_fixed/`.

### Current state observed (reported, not acted on)
- The fixed PDFs are named `*_new.pdf` (02–05) because the originals were **locked open in a viewer** — Windows File Lock Law, Engine Law 1.
- The original PDFs for **02–10 are gone from disk**; **06–10 now have no PDF at all**.
- `note_linter.py` and `STUDY_NOTE_MANIFESTO.md` are untracked; the fixes and `AGENTS.md` §6.0.1 are uncommitted.
- The three locked File-01 variants are still on disk (untracked, gitignored).

---

## ASE Week 02 Units 06–10 — clean rebuild + Unit 10 ordering fix (DONE, awaiting swap + push)

**Per the previous-session lesson, the discipline was applied:** clean markdown → `note_linter.py --strict` (no `--fix`, to avoid the known Pattern C damage — manual asterisk balance instead) → list-separator fix where needed → `pdf_exporter.py -t study_pack --lang ar` (sandbox disabled) → render every page and visually inspect.

### Results (Units 06–09 were rebuilt earlier in the day; Unit 10 just completed)

| Unit | Pages | Diagram page | leaks | asterisks | QA cards |
|:--|--:|--:|--:|--:|:--|
| 06 Spiral | 13 | 4 | 0 | 0 | ✓ |
| 07 Unified Process | 11 | 5 | 0 | 0 | ✓ |
| 08 Agile/XP/Scrum | 15 | 5 | 0 | 0 | ✓ |
| 09 Choosing a Model | — | — | 0 | 0 | ✓ |
| 10 Master Comparison | 29 | **5** (after fix) | 0 | 0 | ✓ |

### Unit 10 specific work
- Added frontmatter, replaced `> **Subject · Week · File:**` with `> **Sources.** …` + `> **Note on method.** …`.
- Restructured "Where this sits" to the 4-part spine (Unit Context / The question this unit answers / The question it hands to the next unit / The connective tissue) — mirroring Units 06–09.
- Created `master_model_comparison.png` via `diagram_forge.py` (11-model table + 2-axes recap, Archimedean-spiral-style layout).

### The Unit 10 ordering bug — caught and fixed
- First insertion placed `### 2.5 The whole series in one picture` BEFORE `## 2. The Two Axes Recap — Why the Series Is One Argument`, making a §2.5 appear before its parent §2 (logically broken).
- Visual inspection of the first render exposed it. Removed the misplaced block, re-inserted after `### 2.4` and before `## 3. Mall's Exercises` — now correctly the capstone of §2.
- Lesson: a heading-level diagram subsection MUST go AFTER the parent's analytical subsections, not before. The engine doesn't enforce this; only render-then-look catches it.

### File Lock Law fired again — Unit 10 canonical PDF locked open
- Corrected PDF generated to `08_PDF_Exports/Week_02_File_10_Master_Comparison_and_Exam_Bank.pdf` is held open in a viewer (PermissionError on mv/cp/Python).
- Per the engine convention, the corrected content lives at `…_new.pdf` (timestamp 13:48, 29 pp, 0 leaks, diagram correctly on p5).
- Student chose to close the viewer and confirm; awaiting his "closed" signal before doing the rename-and-remove swap.

### Pending after swap
- commit + push: markdown 06–10 + diagrams + PDFs + `note_linter.py` + `STUDY_NOTE_MANIFESTO.md` + uncommitted `pdf_exporter.py` / `AGENTS.md §6.0.1` fixes.
- archive Units 02–05 `*_new.pdf` to `99_Archives/` (the other agent's outputs; originals are gone).
