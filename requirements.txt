# Master Studio Core Toolchain & Dashboard Dependencies
# Python 3.10+ compatible

# 1. Office Exporter (Markdown -> DOCX with BiDi RTL + PPTX Native Shapes)
python-docx==1.2.0
python-pptx==1.0.2
pillow==12.0.0

# 2. Arabic Typography & Text Reshaping
arabic-reshaper==3.0.0
python-bidi==0.6.7

# 3. PDF Ingestion & 100% Local CPU OCR (Zero API calls)
pymupdf==1.28.2
pymupdf4llm==1.28.2
rapidocr-onnxruntime==1.4.4
onnxruntime==1.30.0

# 4. Local Web Dashboard
flask==3.1.3

# 5. Testing & CI (pytest runs the suite under tests/)
pytest==8.3.4
