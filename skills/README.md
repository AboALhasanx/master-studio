# Skills (Legacy Location)

The canonical, auto-loaded agent skills now live in:

```
.mimocode/skills/
```

This folder is kept as a reference copy. MiMo Desktop reads skills from `.mimocode/skills/` — edit files there, not here.
